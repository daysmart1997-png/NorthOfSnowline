# 雪线以北 · 界面规范

本轮为现有游戏的简化与统一。依据用户提供的《漫漫长夜》截图，提取白色生存符号、低干扰 HUD、清晰操作层级；不使用其资产或照搬其布局。保留冷灰蓝雪林、护林员装备及原有全部玩法。

## 视觉与信息

- 游戏世界占主画面；游玩时不常驻大标题、英文版本号和重复物资清单。
- 左上显示当前目标；右上显示环境。左下采用两排：上排健康图标、标签和长条；下排体温、体力、水分、饱食、精力。状态图标比首版缩小 25%，保留 14 px 中文短标签和低值文字提示；详细数字在背包。
- 前景雪白 `#edf0ec`，次级冷灰 `#b8c3c7`，炭蓝底 `#17232b`，选择/关键交互用暗金 `#d7b879`；低于 25 的危险状态使用 `#e49a85` 和文字，不能只靠颜色。
- 字体统一 PingFang SC / Microsoft YaHei / Noto Sans CJK SC；字号 14、16、20、28。标题用同族放大，不再混搭宋体和多色数值条。
- 间距以 8 为单位，主屏留白 32，弹层留白 24；按钮高至少 40，具备悬停、按下、禁用和键盘焦点。
- 薄线用于选中和焦点，普通按钮不用厚边框；背包保留物品格以支持拖放，去除缝线、皮边和重复纹理。
- 背包开合采用短暂淡入/淡出，暂停规则不变；地图维持可读的实用勘测图，统一色调与字体。
- 环境栏保持右上两行：天数、时间与昼夜阶段在上，天气、室外温度与区域在下。左上目标下方承载暮色/严寒提醒，低体温警告优先。界面的时间与炉火余量统一使用游戏时间。

## 交互与边界

WASD、方向键、Shift、C、E、F、B、V、M、Tab、H、Esc 的原功能保留。暂停、存取档、制作、磁带拖放、储物箱与手记都必须可达。HUD 仍从 expedition.gd 的单一状态读取，不另建生存数据。

以 1280×720 逻辑视口布局，并使用 Godot canvas_items 缩放；实际检查普通窗口和 Retina 渲染。图标均由本仓库绘制，不需要外部素材。

## 探索线索与休息反馈

房屋不放命名牌。路径引导优先用轮廓、旧物、布条、痕迹；仅在岔路与过桥节点放少量小路标。世界文字固定在实体上、哑光灰白、无描边，短至 2–4 字；长文通过主动查看进入手记，最新线索优先，不公布未发现条目的总数。

休息预览只显示做决定需要的信息，不能使用独立于生存规则的估算。禁用制作按钮直接显示原因。体温趋势不增加新面板；生存栏允许 85–130% 缩放，图标、文字和底部背景共同适配。
