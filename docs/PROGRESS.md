# 当前进度 · 跨设备接续入口

更新时间：2026-09-09。游戏版本：v0.4「地形与林场建筑」。当前交接：将 Mac 上的雪面、两排 HUD、昼夜、探索求生、第一章叙事和室内外围氛围改动统一交付到原仓库 main，供 Windows 接续。源码与 89 个 LFS 资产来自 https://github.com/daysmart1997-png/NorthOfSnowline ，Mac 工作副本为 `/Users/leoduan/Documents/ChatGPT/北`，开发基线 `d22ec96`，main 跟踪 origin/main。下文“未提交/推送”属于各轮结束时的历史状态，以最新 Git 提交和远程分支为准。

## Windows 接续入口（2026-09-09）

- 用户已授权将上述完整改动提交并推送到原仓库 main。当前实现以第一章叙事和室内外围氛围两节为准；纯暗室外背景已被朦胧雪林取代。
- Windows 在原仓库先检查 `git status --short --branch`，确认工作区干净、位于 main 后执行 `git pull --ff-only origin main`、`git lfs pull`、`git lfs fsck`。如有本地修改或分支分歧，先保留并检查，禁止强制覆盖。
- 使用 Godot 4.7.2 标准版打开根目录 project.godot，等待导入后按 F5。运行不依赖 Mac 启动脚本、Python、Blender 或本机 artifacts；不要重建美术资产。
- Mac 已通过 import + 九组检查，已实际检查日夜、室内外与 1080p 画面。Windows 拉取后先运行 tools/run_checks.py，再实看室内外围模糊、进出门脚印、中文 UI 与第一章无线电流程；本轮尚未在 Windows 验证。
- 玩家 savegame.json、设置、截图、性能日志、视频和 Codex 对话不通过 Git 同步；测试工具和复验说明随源码提供。用户已取消录像，继续开发不自动启动录制。
- 下一步优先首次玩家的章节节奏、声音听感、探索线索可读性与帧率验证。房屋不加命名牌，路牌保持少量、风化、克制；核心仍是探索欲望与求生压力。

## 已完成的游戏功能

- 固定斜俯视正交镜头、按屏幕方向移动、奔跑/蹲行、镜头缩放。
- 体温、体力、健康、饱食、水分、精力和 24 kg 负重；14 类物品、7 项配方；9 处发现地点及搜寻物资。
- 无线电零件往返任务；完成任务后继续生存。小屋/车站庇护、添柴炉火、休息、封窗、修床、储物箱、最多三个临时营地。
- 磁带机和三盘音乐：背包内装带、拖放电池/磁带、播放/停止、电量与生存增益。
- 高地、背风洼地、冻湖、过桥；雪深与海拔分开计算，深雪和上坡减速。落脚深浅鞋印、连续趟雪痕迹、降雪逐渐填平。
- 8 段 Mesh2Motion 适配动作，重做对称行走腿部轨迹，实时足部射线与双腿 IK，按坡面调整鞋底。脚步音、呼吸/喘气、白气、风声/炉火与背景配乐。
- 原创林场建筑概念图及 Blender 重建的小屋、室内、木桁架桥、帆布营地；屋顶与近侧墙切开，室内有炉具、床、桌、工具、物资架等。
- 简约炭蓝 UI、原创单色生存图标、六类物品、行囊淡入/淡出、统一勘测图。JSON 存档兼容旧版本，保存生存、物资、探索、修缮、营地和磁带状态。

## 本轮雪面与 UI 优化

- 门外/轨道/林道保留薄雪，脚印不再受地形压平遮罩清零；按脚掌接触位置排除木地板、桥面、冰面和抬高物件，保留左右交替落脚。
- 限制压痕不超过雪深、柔化深雪鞋印和拖痕、薄雪中断深雪连续沟痕；调整路肩和不规则风纹，修正纹素采样及近远雪面的接缝/光照，关闭小雪花硬阴影。
- HUD、行囊、制作、手记、地图、菜单采用同一套字体/单色图标/配色。规则在 `DESIGN.md`，主题与状态显示分离到 `field_theme.gd`、`field_icon.gd`、`survival_hud.gd`，仍读取原生存状态。
- Mac 全套 import + 五组检查通过；新增真实出门脚印回归。检查 1280×720 和 1920×1080 的实际渲染；短时移动最终样本约 40.1 FPS，不能声称稳定 60 FPS。
- 详情与复验入口见 [雪面与 UI 优化](08_SNOW_AND_UI.md)。玩家存档未覆盖，LFS 资产未重建，更改尚未提交/推送。

## 两排 HUD 调整与后续建议

- 按用户第二轮反馈，将状态图标缩小 25%、健康图标约缩小 24%，左下改为上排健康长条、下排五项状态；中文标签字号和低值提示保留，右下行囊信息不变。
- Mac 已检查两排 HUD 的正常/低状态渲染（`artifacts/two-row.png`、`two-row-low.png`），import + 五组检查通过（`artifacts/two-row-checks.log`）。补齐 snow_ui 测试退出前音频混音器的 100 ms 清理等待，与其他集成检查一致，避免加速测试偶发报告 WAV 仍占用；游戏运行逻辑未更改。
- HUD 调整后的建议已获用户“可以的”确认，随后完成下节昼夜与时间光照。Tab 地图继续保留，没有增加常驻小地图或罗盘。

## 基础昼夜与时间光照（2026-09-09）

- 09:00 开始，现实 24 分钟 / 游戏一天。太阳方向、色温、亮度与天空、雾色、环境光同步变化；风雪与昼夜共同影响光照，避免天气更新覆盖夜间亮度。
- 夜间额外降低室外温度 8°C、增加失温；暮色提醒返程，严寒提示庇护所。夜晚保留冷色环境光和不投阴影的弱方向补光，实际截图可辨认角色、树木、深雪鞋印和炉火暖光。
- `expedition.elapsed` 为唯一时钟，`day_cycle.gd` 是纯计算模块。暂停、行囊冻结时间；休息结算 2 个游戏小时并消耗炉火/补给。旧存档兼容，读档恢复相同时间和光照。右上仍为两行环境栏，炉火余量统一显示游戏分钟。
- Mac Godot 4.7.2：import + day_cycle / snow_ui / rules / integration / polish / frontier 六组全部 PASS，规则检查 48 项；包含跨午夜、晨昏连续性、同天气昼夜失温差、真实行囊休息、暂停与存档光照恢复。日志：`artifacts/day-cycle-checks.log`、`artifacts/checks-*.log`。
- 实际 Compatibility 渲染检查小屋的晨光/正午/暮色/深夜，以及夜间室内与深雪脚印。截图 `artifacts/v04-cabin-{dawn,noon,dusk,night}.png`、`v04-interior-night.png`、`v04-hollow-night.png`。没有把 headless 测试等同于画面检查。
- 最终 M2 / 1280×720 移动性能样本（预热 1 秒、采样 6 秒）：白天 219 帧、平均 27.48 ms（36.4 FPS）、P95 35.76 ms；夜晚 292 帧、平均 20.63 ms（48.5 FPS）、P95 22.22 ms。数据为 `artifacts/day-cycle-performance-{day,night}.json`。白天样本较上一轮约 40.1 FPS 偏慢，不能归因于单一改动或声称稳定 60 FPS；下一轮需专项分析动态阴影与雪面开销。
- 原型尚未加入手持灯具、睡眠时长选择、季节/月相或长期天气预报；一天长度及夜间消耗仍需长时间试玩平衡。详细规则与复验入口见 [昼夜与时间光照](09_DAY_CYCLE.md)。玩家存档未写入，二进制资产未重建。

## 完整试玩与 Demo 录像（2026-09-09）

- 完成一次真实游戏场景的自动试玩：小屋出门、门口工具箱、倒木、地图、奔跑/雪坡脚印、邮递车磁带机、装带播放、返家修床封窗、饮水进食、添柴、四次正常休息、夜间出门并返回小屋。无瞬移、物资注入或直接改时钟；未写玩家存档。这不是人工连续按键试玩，也不代表车站任务通关。
- Godot 4.7.2 Movie Maker 原始录制 7013 帧，1280×720 / 30 FPS，成片 03:53.77，实际录制耗时 04:23。输出帧率属于固定时间步进录像参数，不替代此前 M2 实时性能结果。
- 完整未剪辑 MP4：`artifacts/playtest-demo/north-of-snowline-demo.mp4`，约 115 MiB，H.264 / AAC 立体声，14 个章节。原始 AVI、章节截图、状态记录、时间点和建议均在同目录；实际查看菜单、门口、雪坡/地图、行囊、制作与夜间画面。
- 正式录制出现 `DEMO_COMPLETE`，无引擎错误；完整视频/音频解码通过，原声音轨非静音（均值 -35.3 dBFS、峰值 -17.0 dBFS）。主观混音听感、全地图与长期生存平衡仍未人工验证。
- 待改善：白天屋顶亮部偏白；门口薄雪脚印在树影与夜色中不够醒目。预跑的树干阻挡通过绕行解决，未关闭碰撞。录像期间没有修改正式玩法。
- 新增独立的 `tools/playtest_demo.gd` / `.tscn` 与 `tools/export_playtest_demo.py`；`tools/.gdignore` 保留。预跑退出的混音器清理等待已补齐。候选索引审计通过（228 文件、89 个 LFS 资产），真实暂存区未改变。复现与范围见 [试玩说明](10_PLAYTEST_DEMO.md)。

## 试玩反馈后的雪面材质优化（2026-09-09）

- 新增原创 `roof_snow.gdshader`，建筑的 SnowCap 表面共用运行时材质覆盖；压低过亮反射和底色，加入柔和风积纹理。没有调整世界曝光/太阳、没有重新生成或修改 GLB/Blender 二进制资产。
- 薄雪压痕依据真实雪深使用独立颜色渐变和轻微环境光遮蔽，提高树影与夜间的辨识度。压痕深度、左右脚落点、木地板/桥面/冰面排除、深雪拖痕与降雪填平规则不变。
- Mac / Godot 4.7.2：最终 import + day_cycle / snow_ui / rules / integration / polish / frontier 六组均 PASS（`artifacts/snow-finish/checks.log`）。包含真实出门行走、双脚落点、地形/沟痕/填平与日夜回归。
- 使用 `tools/snow_finish_preview.tscn` 检查同一摄像机、相同位置/光照/压痕的早上、正午和夜间前后渲染，并检查夜间已填平一部分的脚印。证据在 `artifacts/snow-finish/before/`、`after/`；没有引擎或着色器错误。早上同一屋面 100×55 像素区域，RGB 均 ≥250 的白色像素比例由 100% 降至 0%，修订后平均 RGB 为约 (175,207,236)。该指标只描述所选区域，不代表整场景曝光。
- M2 / 1280×720 短时移动样本：240 帧，平均 25.14 ms（约 39.8 FPS），P95 26.67 ms；绘制调用 554，与前轮白天样本相同。没有新增纹理采样、灯光或阴影贴图；仍不能声称稳定 60 FPS。数据在 `artifacts/snow-finish/performance.json`。
- 原完整 Demo 保留为发现问题时的版本；本轮交付优化后的代码与对照截图。说明与复验命令见 [雪面材质优化](11_SNOW_FINISH.md)。未提交或推送。

## 探索与求生体验优化（2026-09-09）

- 五项优化全部落实。房屋移除命名牌；八块重复路牌替换成两处实体风化路标，哑光灰白短字、无描边，与木纹、钉头、雪边统一尺度。新增岔路巡林纸页、断绳雪橇/西岭帆布、猎人纸页和布条；主动查看才进入手记，新线索置顶，不公布未发现条目总数。模型均为代码生成，没有改 GLB / Blender 文件。
- 封窗只减缓失温（室内无火损失 ×.35），不再无火回温。室外基础/风雪/夜寒系数调至 .10/.26/.08。休息可选 1/2/4 小时，通过同一规则预测醒来时间、饥渴、体温、炉火和精力；体温 ≤18 或饥渴 ≤5 提前醒来，避免无提示地睡到死亡。
- 拾取/查看 .7 秒、添柴/无线电 .85 秒后结算，进度条和现有动作配合；移动、遮挡/离开目标、暂停和背包会取消，重复 E 不重复结算。制作提前禁用并显示不足/已完成原因。新增体温趋势、实际分组音量与 85–130% 生存栏缩放，设置单独存为 user://preferences.cfg。
- 雪面改用 128² 静态噪声查找纹理，局部网格降为 256×256 分段，仍保留 768² 压痕和动态太阳/8192 阴影图。M2 / 720p 相同短路线：修改前 26.09 ms（38.3 FPS）、P95 30.06 ms；最终 21.84 ms（45.8 FPS）、P95 22.73 ms。帧耗时减少约 16%，不是稳定 60 FPS。对照 4096 阴影图没有可靠收益，未采用。
- import + exploration / day_cycle / snow_ui / rules / integration / polish / frontier 七组检查通过。实际查看两块场景线索、无牌房屋、日夜薄/深雪、制作/休息、手记、设置与放大生存栏。新增 exploration 回归覆盖延迟/中断/重复结算、线索同步、按钮禁用、布局、真实音频总线和设置保存。
- 正常物理与 UI 自动试玩完成车站往返：林道 → 营地 → 木桥 → 车站零件/补给 → 生火休息一小时 → 铁路返家 → 无线电完成。生火组返家体温状态约 57.6，无火对照约 18.9 并触发减速，健康均 100；未瞬移、注入物资或直接改时钟。发现并修正线索 ID 与动态营地 camp_ 前缀冲突。
- 专用录制工具修正 Mac 后台窗口冻结：每模拟帧主动绘制，章节采用帧计数，避免等待截图；导出增加整段冻结检测。30 秒短片实际抽帧确认 5/20/29 秒场景连续改变。该逻辑只在 --recording 工具入口启用，正式游戏不受影响。
- **用户要求先不录制，本轮录像已停止，当前无录制进程，不自动重试。** 新版 MP4 未完成/未交付；最终原始录制停在车站休息阶段，report 标为 interrupted / cancelled_by_user。此前 headless 生火/无火完整往返成功记录仍在 station-rehearsal.log 和 station-cold-control/report.json。截图、性能和检查输出位于 artifacts/exploration。操作和规则细节、复验入口见 [探索与求生体验](12_EXPLORATION_AND_SURVIVAL.md)。原南侧 Demo 保留；玩家 savegame.json 未写入；无提交或推送。

## 第一章叙事串联（2026-09-09）

- 五处打磨已实装为可玩初版：小屋值守簿建立山崩后报平安的动机；岔路/营地/雪橇记录串起送药人与守桥人；取到模块揭示车站撤离；铁路/林道/可选西岭形成返程取舍；无线电需实际完成报告与确认才结束本章。
- 西岭收信簿提供两人的到达回条。结尾依据发现分为收信簿、仅营地纸页、尚未查明三种回应；允许跳过/乱序，后续探索不改写旧回应。北坡信标仅为下一章伏笔，界面明确未开放。
- chapter_one.gd 集中剧情与转移规则，expedition 持有 schema 3 章节状态，chapter_panel.gd 展示共享主题的中文阅读/选择。风险提示独立于滚动正文；阅读暂停，Esc 收起，无线电可断点续接。新增短电流声和实际可见的修复指示窗，没有人物配音。
- 按当前导入桌面校正模块与交接单的摆放，值守簿放在小屋无线电旁；西岭书页独立于可拾取补给箱。房屋无命名牌，不改 GLB/Blender。
- import + chapter / exploration / day_cycle / snow_ui / rules / integration / polish / frontier 八组全部通过。新增通信中磁盘保存/恢复、三种回应、旧/坏存档、重复操作和 UI 边界检查，见 artifacts/chapter/checks.log。保留手动保存，不新增覆盖玩家存档的自动检查点。
- 完整物理/输入/UI 路线：铁路返家约 420.5 秒 / 体温 56.9；绕访西岭约 472 秒 / 体温 40.3，健康均为 100。绕路进入暮色并带回额外信息。属于熟悉路线的脚本试玩，首次玩家节奏待验证。原生渲染检查开场、风险选择、回话、结尾、手记与实体物件。
- 本轮不录制，旧取消录像报告未改。没有提交/推送或写玩家存档。详情见 [第一章叙事](13_CHAPTER_ONE.md)。下一步优先首次玩家和声音听感验证，不把这版称为正式内容锁定。

## 室内视野隔离修复（2026-09-09）

- 修复进屋只切掉屋顶/前墙、仍能看见室外雪地与脚印的问题。新增 scripts/interior_view.gd：室外、两间封闭房屋和人物使用独立渲染层；室内摄像机只渲染当前房屋、室内物品和人物，背景为暗色。物品自身可见性、脚印数据与物理碰撞不随镜头切换改变。
- 房屋内部停止室外太阳/月光的直接投影，保留室内灯具、炉火和随昼夜变化的柔和环境光，避免树影/站外旗杆透过切顶投进屋。出门恢复室外天空、天气、环境和原有雪痕；没有清空脚印。
- 门槛进入范围 3.80 m，离开范围 3.95 m，减少边界微动闪烁。小屋和车站分别隔离；猎人开放帐篷和临时营地保持室外视野。切顶、升级、拾取、章节与读档沿用原逻辑。
- Mac / Godot 4.7.2：import + interior / chapter / exploration / day_cycle / snow_ui / rules / integration / polish / frontier 九组通过，见 artifacts/interior/checks.log。新增回归覆盖真实进出门、产生/保留室外脚印、门槛反复移动、两间屋子、道具拾取、室内读档、新游戏和开放帐篷。
- 原生 OpenGL / Metal Compatibility 实际截图检查小屋、车站、出门雪痕、夜间炉火和开放帐篷，见 artifacts/interior/*.png。新增工具 tools/interior_check.tscn / interior_preview.tscn。未改二进制素材、未写玩家存档、未录制、未提交或推送。

## 室内外围氛围调整（2026-09-09）

- 根据“纯暗外围不符合氛围”的反馈，取代上一轮全暗背景：室内人物/物品清晰，外部保留真实雪林、落雪和日夜色调，以轻微模糊、去饱和、冷雾与柔和压暗退到背景。进出门聚焦约 0.45 秒变化。
- 室内前景继续隔离外部树木与雪痕。新增共享世界的 640×360 氛围视口，复制玩家镜头变换/缩放/宽高比；摄像机远处平面在实际深度之后合成，避免屏幕裁剪误伤室内物件。背景使用粗地形材质副本，不显示脚印或室外文字标记；真实雪痕保留，出门恢复。出门后停止背景视口更新。
- 原生 Mac 720p/1080p 查看白天小屋、车站、夜间炉火、门槛过渡和恢复室外的截图，见 artifacts/interior-atmosphere。脚印没有进入朦胧背景，前景家具/人物没有被一起模糊。
- import + interior / chapter / exploration / day_cycle / snow_ui / rules / integration / polish / frontier 九组通过。interior 增加相机归属与缩放对齐、无雪痕背景材质、室外停止额外渲染和聚焦过渡检查。保留真实进出门、拾取、读档和开放帐篷回归。
- 相同 720p 静止室内、关闭 VSync、主动原生绘制、暖机 1.5 秒后约 6 秒样本：纯背景对照平均 2.87 ms / P95 5.98 ms；氛围版平均 3.69 ms / P95 5.14 ms。此短样本只说明新增背景的开销，不代表全章表现或稳定 FPS。没有录制、提交/推送或写玩家存档。
- 实现及复验见 [室内与外围氛围](14_INTERIOR_ATMOSPHERE.md)。新增 interior_backdrop.gdshader 和 interior_profile 工具；没有改二进制美术素材。

## 当前架构

| 路径 | 职责与关系 |
|---|---|
| `project.godot` → `scenes/main.tscn` → `scripts/main.gd` | 启动、输入、对象组装、HUD、交互、存档和运行时回归入口 |
| `scripts/expedition.gd` | 生存与物品规则、配方、磁带效果、营地和保存数据；elapsed 为唯一游戏时钟 |
| `scripts/interior_view.gd` | 室内清晰前景、朦胧雪林背景、相机对齐与门槛聚焦过渡 |
| `scripts/chapter_one.gd`、`scripts/chapter_panel.gd` | 第一章文本、转移规则与阅读/通信界面；状态归 expedition |
| `scripts/game_preferences.gd`、`scripts/trail_details.gd` | 独立声音/生存栏设置与少量手工放置的探索线索 |
| `scripts/day_cycle.gd` | 时刻、天数、晨昏、太阳轨迹与夜间寒冷系数的纯计算 |
| `scripts/world_frontier.gd` | 当前世界实现：地形、积雪、建筑导入、深雪拖痕 |
| `world_frontier → world_polish → world_expansion → world.gd` | 继承链共同提供地形网格/碰撞、局部雪面、树木、POI、建筑、拾取、天气；旧层仍在使用 |
| `scripts/terrain_profile.gd` | 基岩高度、湖面、积雪、法线、坡度的共享计算 |
| `scripts/player_ranger.gd` → `player.gd` | CharacterBody3D、摄像机、动画播放、落脚事件 |
| `scripts/grounded_feet.gd` | SkeletonModifier3D 双腿 IK 与贴坡 |
| `scripts/backpack.gd`、`inventory_slot.gd`、`item_icon.gd` | 行囊开合、类别、图标、拖放和物品操作 |
| `scripts/cassette_audio.gd` | 磁带、环境、分地面脚步、呼吸和音量混合 |
| `scripts/trail_map.gd` | 统一风格的路线勘测图 |
| `scripts/field_theme.gd`、`field_icon.gd`、`survival_hud.gd` | 共享颜色/字体/交互样式、原创单色图标、精简生存 HUD |
| `assets/` | Godot 直接使用的 GLB、音频、着色器及导入配置 |
| `source_art/` | Blender 源文件、概念图、原始录音、Mesh2Motion 素材及上游说明；有 .gdignore |
| `tools/` | 建模/动画/音频制作、跨平台检查；不是运行游戏的前置生成步骤 |
| `tests/` | 纯规则检查；场景/交互检查也在 main.gd 的命令行测试入口 |

## 本轮仓库整理

- Git 根目录设为实际游戏目录，默认分支 main；设置仓库本地 LFS，不修改全局 Git 配置。
- `.gitignore` 排除秘密配置、依赖、缓存、OS 临时文件、玩家存档、日志截图和备份。保留源码、所有运行资产、Blender 源文件、`.uid`、`.import` 与许可说明。
- `.gitattributes` 设置 UTF-8 文本的 LF 协作约定，并让模型、位图、音视频进入 LFS；`.editorconfig` 记录编辑约定。
- 删除了制作脚本对固定 D 盘和 Windows Python 依赖目录的依赖；没有删除本机依赖。音频再制作依赖记录在 `tools/requirements-audio.txt`。
- 主入口自动创建本地 artifacts 输出目录；中文字体增加 Mac 候选。
- 新增 `AGENTS.md`、`DEVELOPMENT.md`、`ASSETS.md` 和此进度文件。新增可在两端运行的测试入口和暂存区检查工具。

## 验证记录

- v0.4 开发阶段：36 项规则检查、集成检查、背包鼠标拖放与动画/音频检查、地形/沟痕/足部 IK 检查均通过；Windows 实际渲染已检查建筑、行囊、地图及步态关键帧。
- 跨设备整理阶段：Windows 使用 `tools/run_checks.py`，导入及 rules / integration / polish / frontier 全部通过。将暂存区导出到不含旧缓存和本地依赖的迁移目录后，重新导入及四组检查也全部通过；该检查仍是在 Windows 上执行，不代表 Mac 实测。
- 暂存区扫描通过：89 个 LFS 资产，合计 47.30 MiB；没有匹配到常见凭据、依赖目录或大小写路径冲突。五个 Blender 源文件没有外部链接库或未打包的文件图片依赖。Python 源码语法检查、Git 暂存差异空白检查通过。详细输出保存在本地 artifacts，不提交。
- Mac 首次迁移验证已完成，见下节。没有 macOS 导出包，未做签名或公证；当前可通过本机 Godot 和双击启动脚本试玩。

### 2026-09-09 Mac 首次迁移实测

- 平台：Apple M2，arm64，macOS 26.5.2（25F84）；Godot `4.7.2.stable.official.ed1daf0bf`，Git LFS 3.8.0，Python 3.14.0rc1。
- 从原仓库克隆全部 Git 历史并拉取实际资源；`git lfs fsck` 通过。`python3 tools/check_repository.py` 检查 212 个基线索引文件、89 个 LFS 资产（47.30 MiB）通过。
- `python3 -u tools/run_checks.py --godot ~/.local/bin/godot`：import、rules、integration、polish、frontier 全部 PASS；成功标记与错误扫描均通过。包括门口出入、墙体碰撞、背包指针拖放、音频逻辑、步态与雪痕、足部 IK 和地形。日志为 `artifacts/checks-*.log`。
- 实际 OpenGL 4.1 / Metal Compatibility 渲染启动成功；检查 `artifacts/v04-interior.png`、`artifacts/backpack-preview.png` 和运行窗口，确认小屋内饰、人物、雪地、中文 HUD 与背包显示，无相关引擎错误。地图预览命令虽输出成功，但该截图没有地图叠层，因此不将地图视觉检查列为通过。
- `--performance-check` 在 1280×720 下采样 6 秒移动场景：255 帧，平均 23.68 ms（约 42.2 FPS），中位数 21.67 ms，P95 31.25 ms。数据在 `artifacts/v04-performance.json`；仅为短时样本，未达到稳定 60 FPS，也不代表全程或所有 Retina 窗口尺寸。
- 新增 `启动游戏.command`，从脚本目录定位项目、查找 Godot、检查资源导入错误后启动；`bash -n` 通过，已用此入口完成背包截图及正常游戏启动。没有改动玩法代码或重建二进制资产。
- 测试存档仍位于 artifacts，未写玩家 `savegame.json`；Windows 旧存档不在 Git 中，因此没有迁移旧进度。声音的主观听感、长时间探索、桥梁实走及更多高 DPI 尺寸仍待人工试玩。

提交状态：v0.4 跨设备开发基线提交为 bf2ddaf，分支 main；已完成首次推送。已通过用户登录的 GitHub 账户确认提交身份，使用 Leo Duan 和账户的 ID 型 noreply 隐私邮箱，仅写入本仓库的本地 Git 配置。origin 为 https://github.com/daysmart1997-png/NorthOfSnowline.git ，最新进度文档随后续提交同步。

## 当前问题与限制

1. 第一章已有完整可玩叙事初版，尚未经过首次玩家节奏验证/正式内容锁定；没有第二章、人物配音、存档槽 UI 或 Windows/macOS 发布包。
2. 角色造型和手部动作仍需精修；互动已在短接触区间后结算，但尚无逐目标手掌 IK。足部约 22 cm 校正范围，尚无攀爬或专门台阶动作。
3. 雪痕只改变玩家周围 24 m 的渲染网格；物理碰撞使用基础地形，痕迹不保存。冻湖不会破裂或落水；地图等高线为示意。
4. 建造使用预制营地和小屋升级；不能自由拼建，不能逐段造桥。物资容器整组搜寻。
5. UI 基于 1280×720 设计；Mac 已检查 1280×720 和 1920×1080 下的中文、HUD、行囊及地图等画面，声音听感、其他窗口比例和长期键鼠手感仍需试玩；最新 M2 短时移动样本约 45.8 FPS，仍需进一步优化，不能声称稳定 60 FPS。
6. 游戏实际可运行资产已包含；Python 音频依赖在 Mac 的安装/再生成结果尚未验证。Blender 脚本会覆盖目标资源，不能全部盲目重跑。
7. 玩家存档保存在项目根目录 savegame.json，仅本地；如需迁移已有进度应单独复制，不能把它纳入 Git。
8. 世界实现分布于多层继承，main.gd 也承担 UI 和测试。后续适合逐步拆分，但不要在美术任务中顺手大规模重构。

## 下一步开发顺序

1. 先按顶部 Windows 接续入口同步 main 和 LFS 资源，以同版本 Godot 导入、运行九组检查并实际检查画面；Mac 结果不能替代 Windows 复验。
2. 继续 Mac 人工试玩：检查声音听感、长时间探索、极端坡地落脚和不同地点地图标签；根据需求优化 M2 帧率。保留现有验证记录。
3. 精修角色体态、服装层次、落脚与手部交互；保持现有俯视美术，不改变为第一人称。
4. 增加高地/湖岸/林道的手工探索事件，完善物资分布与生存节奏；本轮已加入三处主动查看的线索；后续增加冰裂或天气危险需先确定可读的玩家反馈。
5. 优先对已串联的第一章做首次玩家测试，核对动机、线索发现、返程判断和结尾理解；再根据反馈完善建造/室内用途和磁带剧情，后续计划平台导出。

每次完成工作请在这里补充真实进度与测试结果，使下一台设备能够独立接续。
